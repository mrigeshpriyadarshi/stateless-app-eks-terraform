apiVersion: v1
kind: Service
metadata:
  name: flask-crud-web
  namespace: flask-crud
spec:
  selector:
    app: flask-crud
    role: web
  ports:
    - name: http
      port: 80
      targetPort: gunicorn